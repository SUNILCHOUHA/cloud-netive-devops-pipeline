grafana:
  enabled: true
  adminUser: admin
  adminPassword: gitops123

  service:
    type: LoadBalancer

prometheus:
  prometheusSpec:
    retention: 7d

alertmanager:
  enabled: true

  alertmanagerSpec:
    configSecret: alertmanager-custom-config
